> ⚠️ **测试版（Beta）** — 正式版发布前，所有构建均标记为测试版，可能存在稳定性或功能问题。

## v0.4.0 更新内容

- 修复部分手机插入 U 盘后无法识别：新增 USB Host 支持（`android.hardware.usb.host`、USB_DEVICE_ATTACHED 意图过滤器），插入 U 盘会弹出 USB 访问授权
- 新增「直接模式」：系统未挂载的 U 盘也能打开，FAT32 可读写；exFAT / NTFS / ext2/3/4 / FAT16 / HFS+ 只读
- 主页新增「USB 设备」卡片：清楚显示 未检测到设备 / 需要授权 / 已被系统挂载 / 直接模式已打开 / 格式不支持 等状态
- 新增 U 盘管理：文件系统、卷标、容量 / 已用 / 可用；直接模式下「弹出」与「格式化为 FAT32」（带容量显示、勾选确认和输入「格式化」二次确认）
- 系统挂载的 U 盘提供「系统存储设置」入口（在系统中弹出 / 格式化）

## v0.3.2 更新内容

- U 盘插拔自动感知：停在任意页面（包括主页）插入 U 盘，卷列表 1.5 秒内自动刷新，无需手动点击刷新；回到前台时也会自动重新检测
- 编辑器 / 预览器适配拔出：正在编辑或预览 U 盘上的文件时拔出 U 盘，会提示「文件所在存储卷已移除」并自动返回（编辑器同时放弃未保存状态，避免无法保存时反复弹出确认框）
- 跨盘移动失败不再残留半拷贝文件：复制中途拔出 U 盘会清理目标处的不完整副本并明确报错；复制成功但删除源失败时也会如实提示

## v0.3.1 更新内容

- 修复顶栏上方出现大片空白的问题：安全区 insets 误用物理像素传递给 WebView（WebView CSS px 为 dp），现已按屏幕密度换算，顶栏高度恢复正常

## v0.3.0 更新内容

- 全新 iOS 风格界面重构：顶栏冻结加毛玻璃质感，高度由 52px 降至标准 44px，列表内容可从顶栏下方穿透滚动
- 跟随系统亮 / 暗模式自动切换配色（iOS 系统色：蓝色 #007AFF、红色 #FF3B30、分组灰 #F2F2F7 等）
- 顶栏、底部操作条、侧边栏、弹窗、底部面板统一毛玻璃效果，不支持时自动回退纯色
- 列表改为 iOS 分组样式：内嵌分隔线、更紧凑的行高与排版
- 对话框重构为 iOS 弹窗样式（居中、按钮横排带分隔线）
- 代码高亮、面包屑（›）、文件类型图标配色全面 iOS 化
- 全新应用图标：iOS 蓝渐变 + 白色 U 盘图形，适配 Android 自适应图标与主题图标

## v0.2.0 更新内容

- 新增主页，启动时可自主选择进入「内部存储」或「USB 外接存储」
- 全面屏安全区适配：顶栏、底栏、悬浮按钮不再被状态栏 / 挖孔区域遮挡
- 多选交互优化：长按文件或文件夹进入多选，勾选框仅在选择时显示，返回键优先退出多选
- 全新应用图标：绿色背景 + 白色 USB 字样（自适应图标，适配圆/方形蒙版）
- 侧边栏支持切换存储位置

## 下载说明

- `universal` APK 同时包含 arm64-v8a 与 armeabi-v7a 架构，适合大多数 Android 设备（Android 11+）
- 侧载安装：允许"安装未知应用"后直接安装
- 签名证书：CN=USB File Browser（所有测试版与正式版使用同一签名，可直接覆盖升级）
